# tailwind conformance (M3)

Categorized fixtures — layout/, flex/, grid/, spacing/, sizing/, position/,
overflow/, typography/, colors/, borders/, shadows/, transforms/,
hover-focus/, responsive/, dark/, svg-icons/, forms/ — validated against the
Velqu CSS Profile (`crates/velqu-tailwind`).
