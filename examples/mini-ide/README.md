# mini-ide (M8)

Deliberately limited IDE shell for the M8/M9 benchmark: file explorer, tabs,
editable text, find, command palette, shortcuts, theme, status bar.

No extensions, LSP, debugger, terminal, Git, or AI — the IDE is a benchmark,
not the product.
